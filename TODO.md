## Manual notes (preserved across sync)

- AUD-112-P1-5 [agent] iphone_sensor_agent is lab-only - face/voice/gesture all hardcoded to None at src/bin/iphone_sensor_agent.rs:410-412. Either wire perceive_service or keep header-comment scope declaration. See os/05 lab-only callout and os/112 §3.3.

# nuclear-eye — TODO

> AUTO-GENERATED 2026-05-12 from `~/git/os/PLAN.md` by `~/ai-structure/scripts/sync-todos.py`.
> **Do not edit this file directly.** PRs amend `os/PLAN.md` (or its source docs); rerun the sync.
> Sections included: 1, 4.
> Per `os/PLANNING_DOCS_INDEX.md` (2026-05-12): `os/PLAN.md` is the single rolling plan; per-repo TODOs are extracts.

## 1.0 2026-05-12 audit addendum (`os/112`)

Redo audit after the latest cross-repo changes found the implementation ahead of several numbered docs. Until the rows below close, treat `os/agent-comms/NOW.md`, `os/112-cross-product-reality-audit-2026-05-12.md`, and b450 runtime probes as newer than older chain/port/release-gate wording.

- [ ] **AUD-112-P0-1** Update stale chain/port/release-gate docs: `os/03`, `os/04`, `os/05`, `os/39`, `os/41`, `os/53`, `os/57`, `os/58`, `os/PASS_1C_RUNBOOK`, `os/110`.
- [ ] **AUD-112-P0-2** Refresh `os/agent-comms/BLOCKERS.md` for post-D-full Sentinelle residuals and remove resolved-first ambiguity from the top of the file.
- [ ] **AUD-112-P1-1** Add a b450 listener truth diagram to `os/35`: gateways, chain-signer, FastVLM, perceive, Fortress, Penny.
- [ ] **AUD-112-P1-2** Arianne voice-first closure: full-duplex/call UX, real STT state, clear fallback states, speech-emotion availability.
- [ ] **AUD-112-P1-3** Arianne L1 artifact closure: verify `ArcFace.mlmodelc` App Store bundle phase; bundle or explicitly fallback-gate `LFCNN.mlmodelc`, `EfficientViT_FER.mlmodelc`, and Whisper artifacts.
- [x] **AUD-112-P1-4** Emile sensing boundary: Emile owns enterprise L1 sidecars behind `apps/api-gateway`; renderer/avatar direct kernel bypasses are documented and guarded by `emile/docs/human-sensing-boundary.md` + `emile/scripts/check-emile-sensing-boundary.sh`.
- [ ] **AUD-112-P1-5** Sentinelle iPhone sensor path: wire `iphone_sensor_agent` to real L1 perception fields or declare it lab-only.
- [ ] **AUD-112-P1-6** Sentinelle model maturity: deploy real FER/SER for `perceive_service` or expose heuristic/fallback mode in health.
- [ ] **AUD-112-P1-7** Enforce product-gateway-only ingress in Emile/Arianne/Sentinelle docs and runtime config.
- [x] **AUD-112-P1-8** Finish `nuclear-sdk` 0.5 consumer pin/path-dependency policy across nuclear-eye, nuclear-bootstrap, and aurelia — closed 2026-05-12 with guarded local path deps (`version = "0.5"` + path) and `nuclear-sdk/scripts/check-consumer-sdk-policy.sh`; registry publish remains an operator/release step.
- [ ] **AUD-112-P2-1** Add conformance checks for `os/75`: no raw payload retention, no third lane, telemetry reject tests.
- [ ] **AUD-112-P2-2** Reconcile release gates in `os/69`, `os/73`, `os/75`, and `os/101` against real code/tests/TODO rows.

## 1.1 Phase 0 unblock

- [x] **P0-3** `[K]` `nuclear-deploy/profiles/_kernel/` shared building blocks shipped (`pg.yml`, `valkey.yml`, `qdrant.yml`, `fortress.yml`, `penny.yml`, `consul.yml`, `ollama.yml`, `wrapper.yml`)
- [x] **P0-4** `[P/K]` `nuclear-deploy/profiles/{sentinelle,arianne,emile-fortress}/` SKU overlays scaffolded — Sentinelle `pi5-edge-only|pi5-hybrid|pi5-edge-l1l2|b450-dev` populated; Arianne `cloud-saas|pi5-companion|b450-dev` and Émile `pi5-enterprise|b450-dev` scaffolded with `services:` empty (cloud overlays pending P1-9 routing)

## 1.2 Phase 1 kernel (4 days effort — shared across products)

- [x] **P1-1a** `[K]` `nuclear-sdk` 0.2.0 → 0.3.0; `tenant.rs` (`TenantId` newtype) + `endpoint.rs` (primary/failover state machine, switch on 3 consecutive failures or p95 > 2s, recover after 60s primary green)
- [x] **P1-1b** `[K]` `KernelClient` with mandatory `tenant_id`, per-service `Endpoint`, health probe task; always injects `X-Tenant-Id` header
- [x] **P1-1c** `[K]` Tenant-aware Fortress / Consul / Chain wrappers — shipped in `nuclear-sdk` 0.4.0; `KernelClient::{fortress,consul,chain}()` accessors + `health_all()` parallel probe; per-endpoint failover state machine + `X-Tenant-Id` re-used from P1-1a/1b; backward-compat preserved (Penny-only consumers default to unconfigured Fortress/Consul/Chain → `NuclearError::EndpointNotConfigured`); live integration test `tests/multi_service_failover.rs` proves Fortress flip + tenant header on primary AND failover; 34/34 lib + 5/5 integration tests green
- [x] **P1-1d** `[K]` Integration test `nuclear-sdk/crates/nuclear-sdk/tests/failover.rs` proves live HTTP flip + `X-Tenant-Id` on the wire (also closes §1.4 P5-5)
- [x] **P1-2** `[A]` Kernel `/health` extended with `{brain_state ∈ {normal, cloud_failover, degraded}, tier, failover_url, degraded_since}` on `fortress-net` + `penny-brain` (additive, legacy `status` retained, openapi updated, 12 unit + integration tests green)
- [x] **P1-3** `[K]` Multi-tenant kernel migration: `kernel.tenant_check` helpers (`048`), `riviere.*` `tenant_id` + RLS (`049`), Pass-1a-aware policies on public-schema tables (`040`); `tenant.rs` Pass-1a helper; `tenant_conformance` test extended to riviere
- [x] **P1-4** `[K]` `nuclear-eye/face_db` SQLite → Postgres + pgcrypto AES-256 on embedding BLOBs + bearer auth + `tenant_id NOT NULL` + RLS — Pass 1 (auth/tenant guard) + Pass 2 (`FaceStore` enum + `PostgresFaceStore` behind `face_db_pg` feature, column encryption via `face_db.set_embedding/get_embedding`, `SET LOCAL app.tenant_id` + `app.face_db_key` per txn). Backend selection on `FACE_DB_DATABASE_URL`. 41/41 lib tests green in both feature variants. Closes `os/55` CRITICAL biometric + HIGH face_db auth.
- [x] **P1-5** `[K]` `iphone_sensor_agent` bearer auth + tenant header — `IPHONE_SENSOR_TOKEN` guard on `POST /sensor/iphone`, `X-Tenant-Id` forwarded to alarm_grader. Closes `os/55` HIGH.
- [x] **P1-6** `[K]` `nuclear-wrapper` `brain_monitor` — polls kernel `/health` (P1-2 `brain_state`), publishes `kernel.brain.failover.{cloud,degraded}` + `kernel.brain.recovered` to `riviere.consciousness_stream` + broadcast channel. Opt-in via `KERNEL_BRAIN_HEALTH_URL`; sticky/idempotent transitions, first-observation suppression, Pass 1a/1b legacy-nil tenant default, `X-Tenant-Id` on probe, `SET LOCAL app.tenant_id` per riviere tx. 6/6 brain_monitor + 42/42 core lib tests green.
- [x] **P1-7** `[K]` `kernel.outbox` migration (`050`) + `nuclear-wrapper` outbox worker — multi-tenant write-ahead store with `FOR UPDATE SKIP LOCKED` claim, lease-bounded retries, pluggable `OutboxDispatcher` trait + `HttpPostDispatcher` (longest-prefix registry), exponential backoff (1s → 5min cap), per-row `SET LOCAL app.tenant_id`, RLS via `kernel.tenant_check`, `tenant_conformance` extended to `kernel.outbox`. 9/9 outbox + 51/51 core lib tests green.
- [x] **P1-8** `[K]` Reconcile `nuclear-deploy/provisioning/new-tenant.sh` ↔ `smoke-test.sh` ↔ `TENANT_MODEL.md` (`os/55`) — `TENANT_MODEL.md` rewritten as canonical reference (`kernel.tenant_check`, Pass 1a/1b/1c, all in-scope tables incl. `kernel.outbox` + `face_db.*`). `new-tenant.sh` rewritten (UUID validation/mint, generates per-tenant `face_db.dek` + `face_db_token` + `iphone_sensor_token` + product gateway tokens, `_registry.json`, no per-tenant schemas/roles). `smoke-test.sh` rewritten: (A) kernel helper presence, (B) structural conformance for `public.*` (8) + `riviere.*` (6) + `kernel.outbox` (+ optional `face_db.*` via `--face-db-url`), (C) live RLS round-trip via `kernel.outbox`, (D) Pass-1a/1c flag sanity (4 cases), (E) secrets file 0600 check. Both scripts pass `bash -n`; live mint dry-run produces valid UUID + 0600 secrets + jq registry.
- [x] **P1-9** `[K]` Cloud Penny per-tenant routing — `tenant.rs` ships `TenantId` + `TenantRouting` (model_pref · default_provider · privacy_floor · rate_limit_rpm · monthly_budget_usd · org_id · enabled) + cached `TenantRegistry` + axum `tenant_middleware` on `/route`, `/l1/chat[/multimodal]`, `/l2/route`, `/l3/contemplate`, `/v1/{chat/completions,embeddings,optimize,responses}`; SQLite `tenant_routing` table + `spend_log.tenant_id`; honors `KERNEL_REQUIRE_TENANT_HEADER` (Pass 1a permissive → 1c strict); `dispatch()` honors `model_pref` / `default_provider` / `privacy_floor=local-only` (403 `privacy_floor_violation`); per-tenant spend writeback end-to-end via `WriteEvent::Spend.tenant_id` → `db.log_spend_full(.., tenant_id)`; `/admin/tenants` CRUD (CSRF + session, audit-logged, cache-invalidating); openapi.yml documents `X-Tenant-Id` + 400/402/403/429 on data-plane routes + admin endpoints. Tests: tenant 17/17, dispatch 72/72, integration 60/60.
- [x] **P1-10** `[K]` Failure-injection helper `nuclear-deploy/scripts/inject-failure.sh` shipped — 5 subcommands (`kill-local-penny` · `kill-cloud-link` · `kill-both` · `restore` · `status`); OS-aware (`iptables`/`nft` on Linux, `pfctl` anchor `nuclear-inject` on Darwin); systemd → docker → pgrep supervisor detection; append-only JSONL journal at `/var/lib/nuclear-deploy/inject-failure/journal.jsonl` with reverse-replay restore; safety guards (`/etc/nuclear/PRODUCTION` requires `--force-prod`, refuses to firewall localhost); dry-run by default (`--apply` to mutate); `bash -n` clean; runs on Bash 3.2 (macOS) and Bash 5 (Linux); companion `scripts/SCRIPTS.md` doc.

## 1.3 Phase 3 shared UI kernel packages

- [x] **P3-1** `[K]` `@nuclear/ui-web` in `nuclear-ui/packages/web` — Tailwind v4 + `@theme` mapping to existing `tokens/*.css`, shadcn-style primitives (`Button`, `Card`, `Badge`, `Separator`, `cn`); themes `styles/titanium.css` + `styles/arianne.css`; `npm run typecheck` + `@tailwindcss/cli` compile check
- [x] **P3-2** `[K]` `@nuclear/admin-ui` in `nuclear-ui/packages/admin-ui` — widgets: `BrainStateBanner`, `PgHealthCard`, `OutboxDepthCard`, `EncryptionHealthCard`, `PennyEndpointsCard`, `ConsulLineageCard`, `FortressSoulStatusCard`, `RiviereLagCard`, `MetricTile`; depends on `@nuclear/ui-web`
- [x] **P3-10** `[K]` Move `SENTINELLE_GATEWAY_KEY` / `ARIANNE_GATEWAY_KEY` / `EMILE_GATEWAY_KEY` out of `localStorage` to `httpOnly` cookie via gateway login endpoint (shared `@nuclear/auth` client)

## 1.4 Phase 4/5 shared

- [x] **P4-2** `[K]` Shared iOS `BRAIN STATE` banner Swift Package (reused by `nuclear-watch`, `sentinelle-ios`, future Arianne)
- [x] **P5-2b** `[K]` Tenant-isolation conformance — `nuclear-platform/tests/tenant_conformance.rs` asserts `tenant_id NOT NULL` + RLS enabled + forced + `tenant_isolation` policy on every Fortress `public.*` + `riviere.*` + `kernel.outbox` row covered by migrations; `kernel.tenant_check` presence. Cross-tenant “other tenant sees zero rows” is exercised on writable `kernel.outbox` in `nuclear-deploy/provisioning/smoke-test.sh` §C; optional `face_db.*` structural + round-trip when `--face-db-url` (separate DB per `TENANT_MODEL.md`). Test is `#[ignore]` without `DATABASE_URL` — wire in CI with a Postgres service container to run `cargo test --test tenant_conformance -- --include-ignored`.
- [x] **P5-5** `[K]` Generic kernel-failover integration tests — `nuclear-sdk/crates/nuclear-sdk/tests/failover.rs` + `multi_service_failover.rs` prove `KernelClient` HTTP failover + `X-Tenant-Id` on primary and failover (closes with P1-1d / P1-1c)

## 1.5 Phase 6 docs

- [x] **P6-1** `[K]` `nuclear-deploy/docs/DATA_MAP.md` — where every byte lives per SKU per product
- [x] **P6-2** `[K]` `nuclear-deploy/profiles/README.md` — `_kernel/` building blocks + product overlay pattern + three deployment patterns (A/B/C); links to `TENANT_MODEL.md` + `new-tenant.sh`; `LOCAL_PENNY_URL=http://127.0.0.1:8000` added to `profiles/{sentinelle,arianne,emile-fortress}/env.template` for inject-failure / operator probes; `scripts/SCRIPTS.md` cross-link
- [x] **P6-4** `[K]` `nuclear-deploy/docs/CUSTOMER_CONSENT.md` — cloud-failover acceptance OR LLM-blind waiver + tenant UUID disclosure
- [x] **P6-5** `[K]` `os/41 §3.1.a` version matrix update — landed 2026-04-19 (`nuclear-sdk` **0.4.0**, gateway openapi **1.0.4**, Penny `/health`, PG **RLS / outbox / face_db**, Mermaid + §3.1.c + §A.1/§A.2 fixes). **Follow-up (not blocking):** crates.io semver pin for consumers (replace naked `path = "../nuclear-sdk"` where policy requires); `nuclear-consul` **0.2.0** vs other bins **0.1.0** documented as acceptable until fleet semver.
- [x] **P6-6** `[K]` Mark MASTER_PLAN entries for new kernel work

## 1.6 Pass 1c coordinated flip (after `os/58` Arianne + Émile burn-in)

- [-w] **Kernel pass 1c PR** — **READY TO MERGE 2026-04-28.** Burn-in started 2026-04-23, earliest flip 2026-04-24 (elapsed). Code on branch `kernel-pass-1c`: `AppState.require_tenant_header`, `extract_tenant_id` returns 400 when `KERNEL_REQUIRE_TENANT_HEADER=1`, migration 055 drops DEFAULT + `ALTER DATABASE ... SET "app.kernel_require_tenant_header" = '1'`. Only operator actions remain: see `os/PASS_1C_RUNBOOK.md`. After merge: `sudo systemctl restart fortress.service` on b450.

## 1.7 Cross-cutting security / ops backlog (`os/46` + `os/35 §G` + `os/53`)

Items affecting more than one product, not folded into §1.1–§1.6.

### P0 (`os/46`)

- [x] **T-P0-01** `[K]` `git rm` done (`0a6f468`); file no longer tracked. **Ops done (2026-04-21):** journald audit — no credential values in logs (only provider name references); `FORTRESS_API_TOKEN` rotated in `secrets.toml`, `.env`, `arianne.env` → `e07e4f28-…`; Fortress restarted + healthy. `ANTHROPIC_API_KEY`: dev key — no rotation needed
- [x] **T-P0-02** `[K]` Fail-closed wrapper deployment shape — every kernel binary refuses to start if `nuclear-wrapper` sidecar is unreachable (closes `os/35 §G` "binaries continue running unguarded")
- [x] **T-P0-03** `[K]` Expand `/etc/nuclear/lucky7.toml` to full service list on b450 (Lucky7 has authority over every guardable process)
- [x] **T-P0-04** `[K]` Émile api-gateway has JWT HS256 middleware (`OS_JWT_SECRET`) + fail-closed startup guard (`process.exit(1)` when secret absent + `NODE_ENV !== development`). Already in `server.ts` at startup guard block. Exceeds simple Bearer-key parity.

### P1 (`os/46`, `os/53`)

- [x] **T-P1-12 / T-P1-13** `[K]` Python `nuclear_client` SDK + `emile-avatar` migration (stops calling Fortress directly)
- [x] **T-P1-17** `[K]` Fortress YubiKey PIV — replace placeholder with real ECDSA-P256 verification
- [x] **T-P1-18** `[K]` `X-Nuclear-Token` enforce flip per `os/53 §4` sequence: soak journald → Sentinelle (`FORTRESS_REQUIRE_SERVICE_TOKEN`) → ~7 days → Arianne + Émile gateways
- [x] **T-P1-19 / T-P1-20** `[P]` Sentinelle: rate limiter upgraded to `keyGenerator` → `X-Sentinelle-Key` (fallback IP); Arianne: `chatRateLimit` (30/min per Bearer key) applied to `POST /v1/chat` (W8.2). Arianne global T-P1-20 limiter was already labeled in code.
- [x] **T-P1-22** `[K]` `nuclear-platform` mem/cpu limits in compose — `docker-compose.yml` `deploy.resources.limits` per service (Compose v2+); `docker compose config -q` clean
- [x] **T-P1-23** `[P]` Arianne compose binds kernel ports to `127.0.0.1` only — `arianne/docker-compose.yml` already maps wrapper / api-gateway / fortress / penny-brain / ollama (and voice stacks) to loopback; policy documented in compose header
- [x] **T-P1-24** `[K]` Supply-chain audit CI — `nuclear-platform/.github/workflows/ci.yml`: `cargo audit` + SARIF upload (`github/codeql-action/upload-sarif@v3`, category `cargo-audit`); `python-audit` job (`pip-audit` on all `requirements.txt`, excludes venv/node_modules); `npm-audit` job (matrix: sentinelle/services/gateway, arianne/services/api-gateway, arianne/services/persona, emile/apps/api-gateway; `--audit-level=high`). All informational (`continue-on-error: true`); hard gate follow-up when advisory backlog clears.
- [x] **T-P1-25** `[P]` `nuclear-watch` biometric gating for destructive actions

### P2 (`os/46`)

- [x] **T-P2-02 – T-P2-04** `[K]` Drain endpoints parity, OpenAPI version parity, Lucky7 dashboard `/health`
- [x] **T-P2-05** `[P]` Arianne wrapper sidecar
- [x] **T-P2-06** `[K]` Split `NUCLEAR_CHAIN_URL` (transport vs control plane)
- [x] **T-P2-11 / T-P2-12** `[K]` `TENANT_MODEL.md` smoke-test harness wiring (folds into P1-8); Fortress refuse-without-token in prod (post-T-P1-18 flip)
- [x] **T-P2-13** `[K]` Vault / sealed-secrets adoption — `vault-init.sh` (KV v2 + AppRole + policy + DEK migration), `vault-client.sh` (shell wrapper with AppRole token cache + env fallback), `docker-compose.vault.yml` (Vault container + auto-unseal sidecar, b450 persistent volumes), `fortress-net/src/vault.rs` (`VaultClient`: AppRole login + KV v2 read + env fallback + convenience helpers `service_token/api_token/face_db_dek/face_db_token`), `new-tenant.sh --use-vault` (writes DEK+tokens to Vault KV on provision). 4 lib tests green.

### Operational closure on b450 (`os/13` + `os/35 §G`)

- [x] **OPS-1** `[K]` Loopback binds enforced for L1 (`fortress`, `penny`, `consul`); 0.0.0.0 binds removed from voice/perception/TTS ports — nftables `inet nuclear_loopback` table applied on b450 (15 ports: 3300, 8081–8092, 8100, 8780, 8880); persisted via `/etc/nftables.d/nuclear-loopback.nft` + `nuclear-loopback.service` (oneshot, after docker.service) (2026-04-21)
- [x] **OPS-2** `[K]` Tunnel-exposed L1 (`api.fortress-crew.live`, `consul.fortress-crew.live`) — Caddy 2.6.2 installed; `caddy-auth-gateway.service` running on b450 (:80/:443), `forward_auth` → Fortress `/v1/auth/validate`; stock `caddy.service` disabled (2026-04-21)
- [x] **OPS-3** `[K]` Lucky7 already installed + active as `lucky7.service` (binary `/usr/local/bin/lucky7-daemon`, uptime 3d+); HashiCorp Consul N/A (nuclear-consul is platform's own at :7710); ops verified (2026-04-21)

---

# 2. Arianne

## 4.0 Remaining scope (`os/37`) — at a glance

**§4.1** (`os/56` deep rewire) is largely complete. What follows tracks **`os/37`** integration backlog + ops — **not** §1 kernel package IDs.

| Bucket | Items | Meaning |
|--------|--------|---------|
| **§4.2 — `[x]`** | **P1-1**, **P1-3**, **P2-3** | All verified on b450 (2026-04-21): `sentinelle-hardening-verify.sh` 6/6 pass. |
| **§4.2 — `[-o]` (rolling)** | Sentinelle **P3-1**, **P3-2** | Chain transport vs `CHAIN_ENABLED` + refresh **`os/05-sentinelle.md`** when endpoints change. **Name collision:** these are **Sentinelle-only** IDs — not **§1.3** **P3-1**/**P3-2** (`@nuclear/ui-web` / `@nuclear/admin-ui`). |
| **§4.2 — `[x]`** | **P4-7** | `gesture_pose_mapping.py` + `PerceiveRequest.gesture_pose` + `/v1/perceive/labels` + `test_gesture_pose_mapping.py` (CI) + `sentinelle/services/gateway/src/lib/gesture-pose-mapping.ts` (TS counterpart) all shipped. Arm64 operator soak (`os/37 §9 Appendix A.2`) is exit evidence for retail unlock. |
| **§4.3** | **A1**, **A2**, **A3** — `[x]`; **A5** — `[-w]`; **A6** — `[-o]` | **A1**/**A2** verified on b450 (2026-04-21). **A3** implementation complete. **A5** waits on X5 chain credentials. **A6** = quarterly TestFlight smoke. |

## 4.0.5 os/70 release gate — schema-driven types + 8 contract checks (`os/110`)

Tracks the eight `os/70 §10` release-gate items the 2026-05-02 deep review surfaced. Spec lives at `os/110-sentinelle-os70-release-gate.md` (paste-ready test scaffolds + sequencing). Sprint S1 landed 2026-05-02 even with the Sentinelle code freeze in effect because every artifact sits in out-of-freeze repos (`nuclear-sdk`, `nuclear-apple-sdk`, `nuclear-eye`).

| ID | Item | Repo | Status |
|---|---|---|---|
| **SE-1** | `tests/contract/alert_envelope.test.ts` — round-trip schema validation on every `event.type` (os/70 4.1) | sentinelle | **[x]** 2026-05-02 (Veille `a4327b0`, 13/13 green) |
| **SE-2** | `tests/contract/arianne_boundary.test.ts` — `X-Arianne-*` strip + no affect-vocab in evidence labels | sentinelle | **[x]** 2026-05-02 (Veille `464d9fe`, 17/17 green) |
| **SE-3** | `tests/contract/degraded_reporting.test.ts` — every `degraded.flag` reflected in `/health` + `/readyz` per os/70 9 | sentinelle | **[x]** 2026-05-02 (Veille `bd44fe4`, 25/25 green) |
| **SE-4** | `tests/contract/chain_signing.test.ts` — unsigned alerts rejected at egress in prod profile | sentinelle | **[x]** 2026-05-02 (Veille `d929671`, 28/28 green) |
| **SE-5** | `SentinelleTests/AlertCardUITests.swift` — six `os/70 6` rendering rules enforced (severity, confidence, evidence count, degraded badge, biometric gate, reversibility) | sentinelle-ios | **[x]** 2026-05-03 (`AlertCardViewModel` + 14-test suite) |
| **SE-6** | `tests/AlertCard.test.tsx` — same rendering rules on web | sentinelle-web | **[x]** 2026-05-02 (Veille `8f519ac`) |
| **SE-7** | nuclear-sdk schema-hash pinned + CI drift check across all consumers (TS, Swift × 2, Rust) | nuclear-sdk | **[x]** 2026-05-02 |
| **SE-8** | b450 smoke: synthetic `test_event` end-to-end with chain verification across iOS + web + watch | sentinelle / b450 | **[x]** 2026-05-04 — operator verified: `/api/cameras`, `/api/snapshot/:id`, `/ws/alerts`, arm/disarm, emergency activation all pass; `X-Sentinelle-Key` Keychain-only confirmed; iOS compile/test/UI matrix rerun clean after permanent camera-adapter wiring |

Out-of-freeze deliverables landed 2026-05-02 to unblock SE-1..SE-6:

- `nuclear-sdk/generated/{ts,swift}/sentinelle-alert.{ts,swift}` — canonical types with `schema-hash:` header.
- `nuclear-sdk/scripts/{verify-generated-types.sh,gen-schema-types.sh}` + CI step in `.github/workflows/ci.yml` — closes **SE-7**.
- `nuclear-apple-sdk/Sources/NuclearAppleKit/SentinelleAlert.swift` — sentinelle-ios + nuclear-watch share the type via `import NuclearAppleKit`.
- `nuclear-apple-sdk/Sources/NuclearAppleKit/BrainStateBanner.swift` — closes **1.4 P4-2** (was marked `[x]` but the package didn't exist); 6 unit tests including "no false green" guarantee for unknown values.
- `nuclear-eye/src/alert/{mod.rs,envelope.rs}` — Rust mirror; lets `alarm_grader_agent` move from `serde_json::Value` to typed `SentinelleAlert` whenever G-3 is scheduled. b450-side `cargo build` still pending (M2 build policy defers heavy compile).

**SE-1..SE-8 all `[x]`.** b450 gateway running `--network host` with permanent `sentinelle-camera-adapter` wiring (`ssc-cabled-ch1` at `rtsp://admin:@192.168.2.88/ch0_1.264`). Postgres: `sentinelle-postgres-1` Docker container on host port `5438` (`DATABASE_URL=postgresql://postgres:sentinelle_b450_pg@127.0.0.1:5438/sentinelle`). Note: sentinelle-web main +3 / sentinelle-ios main +10 — operator to push pending commits.

## 4.1 Deep rewire (`os/56`) — Phases 0–6

**See 1 above for all `[K]` tasks** (Phases 0, 1, 3 shared widgets, 4 shared banner, 5 shared tests, 6 docs). Below = Sentinelle-specific `[P]` work only.


### Phase 0 unblock — Sentinelle-specific

- [x] **P0-1** `[P]` `sentinelle-gateway` b450 crashloop fixed — `NUCLEAR_WRAPPER_URL` defaults to `http://nuclear-wrapper:9090` (Docker DNS); retry with exponential backoff (5 attempts: 1/2/4/8/8s) + diagnostic hint if `host.docker.internal` still in use; `.env.example` warning added
- [x] **P0-2** `[P]` `sentinelle openapi.yml` ↔ code reconciled — rate limit corrected to `300/sec`, `/api/alert` rewrites to grader `/ingest`, `/ws/alerts` documented as `alarm_grader_agent /ws` upstream, `/metrics` and `/api/alarm/feedback` (with `AlarmFeedbackRequest` schema) added

### Phase 2 — Product gateway + schema (3d)

- [x] **P2-1** `[P]` `sentinelle-gateway` — `KernelPennyClient` (`services/gateway/src/lib/kernel-penny-client.ts`): `X-Tenant-Id` from `SENTINELLE_TENANT_ID`, primary `KERNEL_PENNY_PRIMARY_URL` / `PENNY_BRAIN_URL`, failover `KERNEL_PENNY_FAILOVER_URL`, 3-strike + slow-response failover, 60s primary recovery probe, `npm test` (3 cases). TS mirrors `nuclear-sdk` semantics (not the Rust crate).
- [x] **P2-2** `[P]` Sentinelle PG — `services/gateway/migrations/sentinelle_001_product_schema.sql`: `sentinelle_cameras`, `sentinelle_alarms`, `sentinelle_perimeter_zones`, `sentinelle_alert_log` + RLS via `kernel.tenant_check`; product DB `SENTINELLE_PRODUCT_DATABASE_URL` / `DATABASE_URL`
- [x] **P2-3** `[P]` `nuclear-eye` `alarm_grader_agent` — `PENNY_GRADER_TIMEOUT_MS` (default 800); `WatchEvent::Alarm.degraded` true when High-path Penny L1 did not apply (timeout / none / error); `cargo check --bin alarm_grader_agent` green *(optional follow-up: nuclear-watch decoders for `degraded`; pre-existing lib test flake in risk_tests noted by agent)*
- [x] **P2-4** `[P]` `sentinelle-gateway` — `GET /health` returns `kernel` (Penny `/health` + `kernel_reachable`, `active_penny_base`) + `product` (`cameras_online`, `last_alarm_at`, `perimeter_armed` via `product-health.ts` + PG); `openapi.yml` v1.0.3

### Phase 3 — Web rebuild (Sentinelle-specific, 5d)

- [x] **P3-3** `[P]` Vite MPA for `sentinelle-web/`: TanStack Router + Query; `vite.config` `rollupOptions.input` → `index.html` + `admin/index.html`; `src/portal/router.tsx` (portal `/`) + `src/admin/router.tsx` (`basepath: /admin`); gateway mounts `SENTINELLE_WEB_DIST` before auth — `/`, `/admin`, `/assets/*`, SPA fallbacks for client routes; `.env.example` + README table
- [x] **P3-4** `[P]` Onboarding wizard (admin) — `sentinelle-web/src/admin/routes/OnboardingWizard.tsx` + `admin/lib/gateway-health.ts` (unauthenticated `GET /health` probe), `mintTenantId()` (`crypto.randomUUID`), `suggestSentinelleTier` + `tierToDeploymentPattern` (A/B/C for `new-tenant.sh`), env snippet `SENTINELLE_TENANT_ID` + `SENTINELLE_TIER`, CLI hint `--tenant-id` + `--pattern`; persist `sentinelle.admin.onboarding.v1` in `localStorage`; route `/admin/onboarding`
- [x] **P3-5** `[P]` Topology page (admin) — `sentinelle-web/src/admin/routes/TopologyPage.tsx` polls `GET /health` every 8s (TanStack Query); `topology-model.ts` builds tiles (gateway drain, Penny L1, optional cloud failover URL, optional Pi #2 when `pi5-edge-l1l2` / tier hint, `product.cameras_online`); tone badges OK / DEGRADED / OFFLINE / STANDBY / UNKNOWN; gateway origin draft + Apply; raw JSON `<details>` for debug
- [x] **P3-6** `[P]` Cameras grid with HLS playback via `hls.js`, snapshot fallback, 4-camera tiles (`/cameras`, `CameraTile` + `xhrSetup` for `X-Sentinelle-Key`)
- [x] **P3-7** `[P]` Alerts inbox with WebSocket subscription to `/ws/alerts`, acknowledge action (`/alerts`, `useLiveGatewayAlerts`, `POST /alarms/acknowledge`)
- [x] **P3-8** `[P]` Sentinelle admin composes `@nuclear/admin-ui` widgets for kernel management + Sentinelle-specific screens (cameras config, perimeter zones, faces list) — local `src/admin/nuclear-admin-ui` + Vite alias to **`nuclear-ui` §1.3 P3-2** package; routes `/admin/kernel`, `/admin/cameras`, `/admin/perimeter`, `/admin/faces`
- [x] **P3-9** `[P]` Tier-switch UI (admin only): swap `SENTINELLE_TIER`, restart gateway, validate failover URLs — `/admin/tier` (live `/health` tier + env/restart copy blocks + browser probe for `failover_url` / custom URL; host must apply env — no gateway mutation API)

### Phase 4 — iOS gaps (2d)

- [x] **P4-1** `[P]` Wire `fetchHomeStatus` to real `/api/home/status` (replaces `unknown` protection mode + hardcoded hero metrics); `X-Tenant-Id` sent when `UserDefaults` `com.noyau.sentinelle.tenantId` is set (Keychain migration = P3-10)
- [x] **P4-3** `[P]` Live Activity: amber on `cloud_failover`, red on `degraded`
- [x] **P4-4** `[P]` App Intents share `AppModel.shared` instead of fresh `AppModel.live()` per invocation (state-loss bug)
- [x] **P4-5** `[P]` Tier-aware Settings labels: "AI Brain: Local Pi #2 (healthy)" / "Cloud (Noyau)" / "DEGRADED — heuristic only"

### Phase 5 — 4-camera + failure-injection (3d, Sentinelle-specific)

- [x] **P5-1** `[P]` Per-SKU smoke matrix: arm/disarm, 4-camera live grid, alarm trigger, ack flow, snapshot fetch (`sentinelle/tests/sku-smoke.sh`)
- [x] **P5-2** `[P]` Failure-injection per SKU using `P1-10` helper: kill Pi #2, kill cloud, kill both → assert no missed alarm, banner ≤5s, recovery ≤90s, no duplicates (`sentinelle/tests/failover.sh`)
- [x] **P5-3** `[P]` Latency budget: grader p95 <800ms (heuristic), Penny call p95 <2s, failover transition <15s (`sentinelle/tests/latency.sh`)
- [x] **P5-4** `[P]` Audit-log assertion: every degraded alarm carries `degraded: true` + reason code (`tests/gateway.test.ts`)

### Phase 6 — Docs

- [x] **P6-3** `[P]` Update `os/05-sentinelle.md` with SKU table + safety-failover principle + tenancy model

## 4.2 Sentinelle integration backlog (`os/37`)

### P0 — broken / mismatched contracts

- [x] **P0-3** Integration test or smoke: WS through gateway; POST feedback; grader logs / `sentinelle.feedback` — Jest now covers gateway+ws+chain plus automated La Rivière/Fortress log assertions via `tests/la-riviere-assertions.test.ts`

### P1 — security / abuse surface

- [x] **P1-1** `ALARM_GRADER_FEEDBACK_TOKEN` enforced on b450: `/feedback` returns 401 (unauth) and non-401 (authenticated) — `sentinelle-hardening-verify.sh` p1-1-* pass (2026-04-21). alarm_grader_agent rebuilt with auth-before-body fix (Bytes extractor); token in `/data/git/nuclear-platform/.env`.
- [x] **P1-3** `/drain` authentication — gateway `SENTINELLE_DRAIN_TOKEN` wired + verified on b450: unauth → 401, auth → 200; `sentinelle-hardening-verify.sh` passes p1-3-* (2026-04-21)

### P2 — reliability / dependencies / observability

- [x] **P2-3** `FASTVLM_API_TOKEN` enforced on b450: fastvlm-service runs at `:8091` (not :8092 — nuclear-camera owns :8092; verify script URL corrected); middleware bearer-guard replaced inline `_check_token` (b450 commit 3214c39); `sentinelle-hardening.env` injected via drop-in `fastvlm.service.d/token.conf`; `sentinelle-hardening-verify.sh` p2-3-* pass (2026-04-21).

### P3 — product / maintainability

- [-o] **P3-1** Chain transport migration vs `CHAIN_ENABLED` story — gateway / grader / iOS all trim-and-case-insensitive; full migration gated on X5 chain credentials
- [-o] **P3-2** Refresh `os/05-sentinelle.md` when endpoints change (ongoing discipline). **Updated 2026-04-21:** added native-b450 fastvlm row (`:8091` systemd service vs `:8092` compose-only); clarified port split in `os/35` §B.1 + port table.

### P4 — follow-ups

- [x] **P4-2** nuclear-scout `Network/*` full review — completed on `NuclearEye/Network/{FortressBridge,ChainTransport,PinningSessionDelegate}.swift`; chain/fortress credential trim + gating + SPKI pinning validated. `ChainTransport` hardened: removed malformed-URL crash paths (`URL(string: ...)!`) via normalized fallback URL parsing.
- [x] **P4-4** Automated La Rivière / Fortress log assertions — `sentinelle/tests/la-riviere-assertions.test.ts` adds schema + stub-server assertions for `sentinelle.feedback` / Fortress audit paths (gateway self-skip when unreachable; full E2E when `RIVIERE_STUB_URL` / `FORTRESS_STUB_URL` are set)
- [x] **P4-7** Scout → appliance perception parity (`os/38 §9.3`): **`nuclear-eye`** ships `gesture_pose_mapping.py`, `PerceiveRequest.gesture_pose` / `gesture_pose_confidence`, `GET /v1/perceive/labels`, and **`test_gesture_pose_mapping.py`** (CI `python-smoke`); `compute_risk` includes **`fast_approach`** / **`hands_raised`** weights. **Sentinelle TS counterpart:** `sentinelle/services/gateway/src/lib/gesture-pose-mapping.ts` (`mapGesturePose`, `buildGestureDict`, `GESTURE_POSE_MAP`, `passthroughEnabled`). **Arm64 soak evidence tooling:** `nuclear-deploy/provisioning/gesture-pose-soak.sh` + `gesture-pose-soak-bundle.sh` (timestamped artifact bundle for A3 sign-off), referenced in `os/37 §9 Appendix A.2`.

## 4.3 Action plan (`os/37 §6`) — sequenced

- [x] **A1** `ALARM_GRADER_FEEDBACK_TOKEN` on b450: alarm_grader_agent rebuilt (auth-before-body Bytes fix); token in `/data/git/nuclear-platform/.env`; `/feedback` → 401 (unauth) / non-401 (auth). `sentinelle-hardening-verify.sh` 6/6 pass (2026-04-21).
- [x] **A2** `FASTVLM_API_TOKEN` enforced on b450: fastvlm-service middleware bearer-guard (b450 commit 3214c39); `sentinelle-hardening.env` injected via drop-in; `/describe` → 401 (unauth). `sentinelle-hardening-verify.sh` 6/6 pass (2026-04-21). Note: fastvlm runs on :8091 (not :8092); verify script URL corrected in `nuclear-deploy`.
- [x] **A3** `P4-7` / `os/38 §9.3` — **`gesture_pose`** path + mapping module + **`test_gesture_pose_mapping.py`** in CI; `sentinelle/services/gateway/src/lib/gesture-pose-mapping.ts` TS counterpart shipped; `sentinelle/docs/gesture-pose-arm64-note.md` arm64 + NCNN doc shipped; `os/37` §9 Appendix A.1 adopted mapping table + Appendix A.2 soak runbook in tree
- [x] **A4** `P0-3` / `P4-4` — Jest coverage extended with `tests/la-riviere-assertions.test.ts` (stub grader consumer / log assertions for `sentinelle.feedback`, degraded reason_code, Fortress audit flow)
- [-w] **A5** `P3-1` X5 — when chain credentials exist, run `CHAIN_ENABLED=true` smoke across gateway + grader + iOS; update `os/05` "remaining gaps"
- [-o] **A6** Quarterly — TestFlight `sentinelle-ios` smoke (cameras, snapshot/stream, `/ws/alerts`, emergency hold → gateway) — **P3-3** regression. **2026-05-04 cycle verified on b450**: `/api/cameras` (ssc-cabled-ch1), `/api/snapshot/:id`, `/ws/alerts`, arm/disarm, emergency activation all pass; `X-Sentinelle-Key` Keychain-only confirmed; iOS compile/test/UI matrix green after permanent camera-adapter wiring.

**Frozen:** no new retail features requiring `nuclear-scout`; Scout changes lab-only. P4-7 implementation complete; operator arm64 field soak (`os/37 §9 Appendix A.2`) is exit evidence for retail unlock.

---

# 5. Exit gates — flip `KERNEL_REQUIRE_TENANT_HEADER=1` (pass 1c)

Cannot flip until ALL of the below are green on b450:

1. `os/56` Phase 1 — **ALL SHIPPED**: P1-1a/1b/1c/1d · P1-2 · P1-3 · P1-4 · P1-5 · P1-6 · P1-7 · P1-8 · P1-9 · P1-10. Phase 1 of the rewire is complete.
2. Arianne adoption (§2.4) — **A1–A8**, ≥1 release-window burn-in
3. Émile adoption (§3.3) — **E1–E8**, ≥1 release-window burn-in
4. Tenant-isolation conformance (**P5-2b**) — **green:** `tenant_conformance.rs` (Fortress + riviere + `kernel.outbox`) + `smoke-test.sh` round-trip on `kernel.outbox` + optional `face_db.*` via `--face-db-url`. **Follow-up (non-blocking for 1c):** add CI job with `DATABASE_URL` + optional `FACE_DB_DATABASE_URL` so ignored tests run automatically; Sentinelle app-layer tenancy has no additional kernel-DB `sentinelle_*` tables beyond the contract above.
5. Generic kernel-failover test (**P5-5**) — **shipped** as `nuclear-sdk/crates/nuclear-sdk/tests/failover.rs` + `multi_service_failover.rs` (P1-1d / P1-1c); ensure integration tests run in `nuclear-sdk` CI.
6. `T-P1-18` X-Nuclear-Token enforce flip per `os/53 §4` sequence reaches Émile + Arianne (Sentinelle first, then ~7d soak)

Then the **single coordinated pass 1c PR** flips the flag, drops the default, enforces missing-header → 400.

---

# 6. Shipped history reference

**Do not duplicate — read `os/MASTER_PLAN.md`.** Tracks A–KL (239/239 rows, 100%). Anything in that doc is *already shipped*; this PLAN tracks what remains.

Significant aggregates shipped and therefore not in this doc:
- Tracks B, D, G, H, I, L, M, O, T, U, Y, Z, AA, BB, CC, DD, FF — fully complete (see `os/status.md`)
- `os/archive/plans/L1-affect-driven-tone-plan.md` — shipped 2026-04-13 as `implemented-v1`
- All `os/archive/` entries — superseded / merged, kept for reference only

---

---

# 5. Cross-cutting closures (2026-04-26 autonomous run)

> **Why a separate section?** The work below landed across nine repos but is
> tracked in `os/87`, `os/91`, `os/93`, `os/94`, and `os/96` rather than as
> numbered task IDs in §1–§4. This section is the single index from PLAN.md
> back to those docs so the canonical task list stays trustworthy.
>
> Authoritative single-page record: `os/96-2026-04-26-autonomous-run-closeout.md`.
